console.clear();

const form = document.querySelector('[data-js="form"]');
const todoList = document.querySelector('[data-js="todoList"]');

form.addEventListener("submit, (e) => "{
    e.preventDefault();
form.reset();
e.target.elements.headline.focus();

cost data = Object.fromEntries(new FormDatya(e.target));
const data = Object.fromEntries(formData);
form.reset();
e.target.elements..headline.focus();
});

function createHtml(dataReceived){
    const createToDo = document.createElement("li")
    const createHeadline = document.createElement("h1")
    const createTask = document.createElement("p")
    

    console.log ("dataReceived Headline", data.Received.headline);
    console.log ("dataReceived Headline", data.Received.task);
}